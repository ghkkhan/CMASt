#include "../includes/main.hpp"

/*
 * This function starts the prologue out. 
 * It returns a number, denoting where the player stopped.... this number is then sent back to the main function
 * The number is used as a checkpoint... 
 * in the case of -1 being sent back, it means that the prolog was read till the end...
 *
 */

int prolog() {
    return 1;
}
